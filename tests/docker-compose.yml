version: '3'
services:
  archlinux:
    build:
      context: .
      dockerfile: Dockerfile-archlinux
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
  centos:
    build:
      context: .
      dockerfile: Dockerfile-centos
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
  centos-old:
    build:
      context: .
      dockerfile: Dockerfile-centos-old
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
  alpine-old:
    build:
      context: .
      dockerfile: Dockerfile-alpine-old
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile-ubuntu
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
  ubuntu-old:
    build:
      context: .
      dockerfile: Dockerfile-ubuntu-old
    volumes:
      - ../bin/nvh:/usr/local/bin/nvh
      - ../../n/bin/n:/usr/local/bin/n
